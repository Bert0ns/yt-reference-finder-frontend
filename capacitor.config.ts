import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.referencefinder.app',
  appName: 'Reference Finder',
  webDir: '.next',
};

export default config;
